<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.neefull.fsp.web.system.mapper.ProjectSettlementMapper">
    <update id="settleUserAmount">
        update t_project_settlement set state = '2',settled_time=NOW() where project_id = #{projectId} and user_id = #{userId}
    </update>

    <select id="getSettleByProjectIdAndUserId" parameterType="String" resultType="String">
        select IFNULL(state,'0') state from t_project_settlement where project_id = #{projectId} and user_id = #{userId}
    </select>
</mapper>
