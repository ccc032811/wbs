<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.neefull.fsp.app.mapper.MsgMapper">

    <select id="getUserMsgs" resultType="MsgInfo">
            SELECT
              a.id,
              b.user_id userId,
              b.read_state readState,
              a.title,
              a.content,
              a.msg_type,
              a.create_time
            FROM
              t_msg_info a
              LEFT JOIN t_msg_user b
                ON a.id = b.msg_id
            WHERE b.user_id = #{msgInfo.userId}
              AND b.delete_state = 0
              AND a.msg_type = #{msgInfo.msgType}
    </select>


</mapper>
